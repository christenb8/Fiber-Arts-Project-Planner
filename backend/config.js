const mongoose = require('mongoose');

/* Here goes the mongoDB Connection and all that */

// config.js

const config = {
    mongoURI: 'mongodb+srv://christenborrero_db_user:Pinnacle1@cluster0.x3jwyzy.mongodb.net/fiber_app',
    port: 4000
}
module.exports = config;